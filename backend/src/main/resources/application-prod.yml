spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/smboutique_local}
    username: smboutique
    password: smbpass


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
