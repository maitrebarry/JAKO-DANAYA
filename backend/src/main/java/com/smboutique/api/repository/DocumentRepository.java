package com.smboutique.api.repository;

/**
 * DocumentRepository removed: documents are generated on-demand and are not persisted.
 * Left as a placeholder to avoid accidental usage; do not extend JpaRepository here.
 */
public interface DocumentRepository {
    // intentionally empty
}