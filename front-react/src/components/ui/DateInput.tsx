// DEPRECATED: UI wrapper removed per user request. Kept stub to avoid breaking imports.
import React from 'react';
const DateInput = (props: any) => <input type="date" {...props} />;
export default DateInput;
