// DEPRECATED: UI wrapper created earlier — removed per user request. This stub remains to avoid breaking imports.
import React from 'react';
const Button = (props: any) => <button {...props} />;
export default Button;
